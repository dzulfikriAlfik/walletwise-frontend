# Production - copy to .env.production and set your values
# Used when running: npm run build (--mode production)

# Backend API base URL (include /api path)
VITE_API_BASE_URL=https://api.walletwise.pintarware.com/api

# API request timeout in milliseconds (optional)
VITE_API_TIMEOUT=30000
